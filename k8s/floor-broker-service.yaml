apiVersion: v1
kind: Service
metadata:
  name: floor-broker
  namespace: multi-agent-ai-trader
spec:
  type: ClusterIP
  selector:
    app: floor-broker
  ports:
    - name: http
      port: 8000
      targetPort: 8000
